"""
Agents package.

The onboarding voice agent is now handled by Vapi (see services/vapi_service.py).
The interview agent (interview_agent.py) still uses LiveKit for candidate interviews.
"""

# Onboarding agent moved to Vapi - no LiveKit agent needed
# Interview agent is in interview_agent.py (still uses LiveKit)
